// logStringArray
// La funcion logStringArray recibe como parametro un arrary de letras 
// tu reto es usar esta funcion para concatenera todas las letras del array y luego imprimir el resultado 

// ojo! solo se aceptan arrays de strings si el array contiene numeros se debe devolver un false

const concatArrayToString = (array) => {
}

module.exports = concatArrayToString;
