const sum = (a, b) => {
  // escribe una función que reciba dos números y devuelva la suma de ambos
  // recuerda que alguno de los dos numeros puede ser un string y debe convertirse a numero
  // si alguno de los valores no es un numero, debe devolver 0
  // si algunos de los numeros es negativo, debe convertirlo a positivo
}


module.exports = sum;