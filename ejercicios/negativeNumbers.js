// la funcion negativeNumbers recibe como parametro un arrary de numeros
// pero hay problema, y es que en el array puede tener numeros negativos o espacios en blanco
// tu tarea, si decides aceptarlo: es devolver un array con todos los numeros positivos y sin espacios en blanco

// Math.abs()

const negativeNumbers = (array) => {
}

module.exports = negativeNumbers;